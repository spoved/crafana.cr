require "../../spec_helper"

describe Crafana::Api::Client do
  it "can gather users" do
    # load_cassette("users") do
    #   client.users.should be_a(Array(Crafana::User))
    #   client.users.size.should eq 1
    # end
  end
end
